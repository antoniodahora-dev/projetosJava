<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestApp">
        <activity
            android:name=".aulaLib.LobLibActivity"
            android:exported="false" />
        <activity android:name=".aulaDesignPatterns.DesignPattersActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".aulaActivity.SecundTaskActivity" />
        <activity android:name=".aulaActivity.ListCompraActivity" />
        <activity android:name=".customView.ViewAndroidActivity" />
        <activity android:name=".viewPager.ViewPager" />
        <activity android:name=".MainActivity3" />

        <service
            android:name=".NotifierListener"
            android:label="AppBot"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>